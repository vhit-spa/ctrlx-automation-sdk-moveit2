name: moveit2-base-humble
title: MoveIt2 snap for ctrlX
version: '1.1.2'

summary: Provides MoveIt2 and ros2_control functionalities for ctrlX CORE

description: |
  MoveIt2 overlay snap running on top of ros2-base-humble.

base: core22

grade: devel
confinement: strict

architectures:
  - build-on: [amd64, arm64]
    build-for: [amd64]
  - build-on: [amd64, arm64]
    build-for: [arm64]

parts:
  moveit-base:
    plugin: dump
    source: ./install
    stage-packages:
      # MoveIt2
      # - ros-humble-moveit
      - ros-humble-moveit-core
      - ros-humble-moveit-ros-planning
      - ros-humble-moveit-ros-move-group
      - ros-humble-moveit-planners-ompl
      - ros-humble-moveit-kinematics
      - ros-humble-moveit-simple-controller-manager
      # - ros-humble-moveit-ros-perception
      # - ros-humble-moveit-servo
      # - ros-humble-moveit-visual-tools

      # ros2_control
      - ros-humble-ros2-control
      - ros-humble-ros2-controllers
      - ros-humble-gripper-controllers

      # Utilities
      - ros-humble-xacro

      # DDS plugin
      - ros-humble-rmw-cyclonedds-cpp

  scripts:
    plugin: dump
    source: ./scripts
    organize:
      '*': usr/bin/

slots:
  moveit-base:
    interface: content
    content: moveit-runtime
    read:
      - /

plugs:
  ros-base:
    interface: content
    content: executables
    target: $SNAP/rosruntime
  datalayer:
    interface: content
    content: datalayer
    target: $SNAP_DATA/.datalayer